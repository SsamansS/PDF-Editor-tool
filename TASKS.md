# TASKS.md — Трекер задач PDF Editor Tool

Единый источник правды по всем задачам проекта. Любая работа над проектом начинается
с открытия этого файла и заканчивается обновлением статуса здесь.

---

## Как трекается исполнение

**Источник правды — этот файл.** Перед началом задачи открываем `TASKS.md`,
после завершения — обновляем статус и пишем строку в «Журнал».

### Статусы
| Значок | Статус | Значение |
|--------|--------|----------|
| ☐ | TODO | не начато |
| 🔄 | IN PROGRESS | в работе (одновременно держим максимум одну задачу) |
| ✅ | DONE | сделано **и проверено** по критерию готовности (DoD) |
| ⛔ | BLOCKED | заблокировано — причина рядом |
| ❌ | DROPPED | решили не делать — причина в Журнале |

### Правила ведения
1. Задачи сгруппированы в **эпики** (`E1`, `E2`, …). У каждой задачи свой **ID** (`T1`, `T1.2`, …) — уникальный в пределах файла.
2. На ID ссылаемся в коммитах: `[T2] add upload endpoint`.
3. Статус меняем синхронно: в **таблице-обзоре эпика** и в заголовке самой задачи.
4. Задача → `✅ DONE` только когда выполнен её **DoD (Definition of Done)**, а не когда «код написан».
5. Каждое изменение статуса фиксируем строкой в разделе **Журнал выполнения** (дату беру из системы на момент записи).
6. Активную задачу дублирую во встроенный трекер Claude Code (TaskCreate/TaskUpdate) — для оперативной видимости в сессии. `TASKS.md` = долговременная память, трекер = оперативная.
7. Новые задачи, всплывшие по ходу, добавляем сюда с новым ID — не держим в голове.
8. Новый крупный блок работ = новый эпик (`E2`, `E3`, …) со своей таблицей-обзором.

---

# E1 — Публикация в вебе на поддомене `pdf.runpro.in`

Цель: коллеги пользуются инструментом через браузер по адресу **`https://pdf.runpro.in`** —
безопасно, с HTTPS, не затрагивая существующий сайт и API на этом же сервере.

> **Решение по способу публикации: Вариант A — поддомен** (подтверждено 2026-06-23).
> Домен `runpro.in` с валидным SSL уже есть на сервере — покупать ничего не нужно.
> Подпуть `runpro.in/pdf/` отклонён: наш app использует абсолютные пути `/api/`, `/static/`,
> а `/api/` на nginx уже занят Java-бэкендом → конфликт. Поддомен даёт чистое разделение.

> Контекст приложения: сейчас оно локальное. Сервер слушает `127.0.0.1:8765`
> (`pdf_editor_web.py:437`), файлы открываются по **пути на диске** (`_resolve_pdf`, поле
> «Полный путь к PDF…» в `index.html:13`), авторизации нет, бэкенд — встроенный `http.server`.
> «Выложить как есть» нельзя: коллега не сможет выбрать свой файл, и доступ будет открыт всем.

## Факты о сервере (аудит T0, 2026-06-23)
- **Хост:** `ubuntu@13.59.170.121`, Ubuntu 24.04, 2 CPU, 7.6 ГБ RAM (6.4 свободно), диск 14 ГБ свободно.
- **nginx** (80, 443): обслуживает `runpro.in` / `www.runpro.in` с SSL Let's Encrypt (Certbot).
  `/` → SPA из `/var/www/runpro`; `/api/`, `/auth/` → proxy на `localhost:8080`.
  Конфиг: `/etc/nginx/sites-available/runpro` (симлинк в `sites-enabled`).
- **Spring Boot** `bot.jar` (8080): backend, сервис `runpro-bot.service`, путь `/opt/runpro/backend`.
- **PostgreSQL 16** (5432, localhost) · **MinIO** (9000/9001, localhost).
- **Python 3.12** системный. **Свободный порт для нас: `8765`.**
- ⚠️ Неинтерактивный SSH имеет урезанный `PATH` — в командах задавать
  `export PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin`.

## 🚧 Guardrails — что НЕЛЬЗЯ трогать (нарушит работающий сайт/API)
- **Порты** 80, 443, 8080, 9000, 9001, 5432, 22 — не занимать, не переназначать.
- **Сервисы** `nginx`, `runpro-bot`, `postgresql@16-main`, `minio` — не останавливать/менять.
- **nginx-конфиг**: только **добавить новый** `server`-блок для `pdf.runpro.in`; блок `runpro.in`
  (его `/`, `/api/`, `/auth/`, ssl) **не редактировать**. После правок — `sudo nginx -t` перед reload.
- **Наш Python-процесс** слушает **только `127.0.0.1:8765`** (за nginx), НЕ `0.0.0.0`.
- Каталоги `/var/www/runpro` и `/opt/runpro/backend` — не трогать. Наш код — в отдельном каталоге.
- Новый systemd-юнит с уникальным именем `pdf-editor.service`.
- В Security Group **новые порты НЕ открывать** (80/443 уже открыты, трафик идёт через nginx).

### Обзор E1
| ID | Задача | Приоритет | Статус |
|----|--------|-----------|--------|
| **T0** | Аудит сервера перед деплоем | 🔴 крит. | ✅ DONE |
| **T1** | Решение по архитектуре доступа к файлам | 🔴 крит. | ✅ DONE |
| **T2** | Загрузка PDF через браузер + скачивание результата | 🔴 крит. | ✅ DONE |
| **T3** | Авторизация (логин/пароль) | 🔴 крит. | ☐ TODO |
| **T4** | Безопасность: убрать path traversal, изолировать рабочую папку | 🟠 высок. | ☐ TODO |
| **T5** | Конфиг через env, bind на `127.0.0.1` | 🟠 высок. | ☐ TODO |
| **T6** | Деплой под `pdf.runpro.in` (DNS + nginx + certbot + systemd) | 🟠 высок. | ☐ TODO |
| **T7** | Автозапуск как сервис (systemd) + логи | 🟡 средн. | ☐ TODO |

Зависимости: **T2 → T1**, **T6 → T2, T3, T4, T5**, **T7 → T6**.
HTTPS больше не отдельная задача — выдаётся certbot'ом внутри T6.5 (переиспользуем готовый Certbot).

### T0 — Аудит сервера ✅ DONE
Собрана конфигурация сервера (см. раздел «Факты о сервере» выше). Подтверждено: порт 8765 свободен,
есть домен `runpro.in` + Certbot, ресурсов достаточно. Выбран Вариант A (поддомен).

### T1 — Решение по архитектуре доступа к файлам ✅ DONE
**Решение: Вариант A — upload через браузер.** Коллега загружает PDF из браузера → сервер кладёт
во временную папку сессии → работает там → отдаёт результат на скачивание. Server-side путь к файлу
(текущий механизм) для веба не подходит — файла коллеги на сервере нет. Реализуется в T2.

### T2 — Загрузка и скачивание файлов 🔴 ✅ DONE
*(зависит от T1)*
- T2.1 ✅ Бэкенд: `POST /api/upload` (multipart) → сохранение в `_uploads/<doc_id>/original.pdf`.
- T2.2 ✅ Изоляция по `doc_id` (uuid4 hex, валидируется regex); клиент НЕ шлёт пути к файлам — везде токен `doc`.
- T2.3 ✅ `GET /download?doc=...` — отдаёт `_edited.pdf` с `Content-Disposition` (имя из исходного файла).
- T2.4 ✅ Фронтенд: поле «путь к PDF» удалено, теперь «📁 Загрузить PDF» (`FormData` → `/api/upload`) + кнопка «💾 Сохранить».
- T2.5 ✅ Очистка: `_cleanup_old_docs()` сносит загрузки старше 24ч (TTL), вызывается при старте и при каждой загрузке.

**Файлы:** `pdf_editor_web.py`, `static/index.html`, `static/editor.js`, `static/editor.css`, `.gitignore`, `AGENTS.md`.
**DoD:** ✅ Проверено дымовым тестом: upload→info→blocks→serve→replace→undo→download проходят; path-traversal (`../../`) и неизвестный `doc` отбиваются `400`.
**Примечание:** legacy-эндпоинт `/action` всё ещё принимает путь к файлу (`_resolve_pdf`) — закрыть/убрать в рамках T4 перед публичным деплоем.

### T3 — Авторизация 🔴 ☐ TODO
Два варианта реализации (выбрать при старте задачи):
- **3a) nginx `auth_basic`** — авторизацию держит nginx в server-блоке `pdf.runpro.in` (`.htpasswd`).
  Проще, не трогаем Python-код. *(предпочтительно для быстрого старта)*
- **3b) В приложении** — Basic Auth/форма в `pdf_editor_web.py`, учётки из env.

- T3.1 ☐ Выбрать вариант, завести учётки **вне кода/гита** (env или `.htpasswd`).
- T3.2 ☐ Закрыть авторизацией **весь** `pdf.runpro.in` (все пути).

**DoD:** без верных учётных данных любой запрос к `pdf.runpro.in` возвращает `401`; учётки вне кода.

### T4 — Безопасность доступа к файлам 🟠 ☐ TODO
- T4.1 ☐ В `_resolve_pdf` запретить выход за пределы `workspace/<session>/` (нормализация + проверка); сейчас принимаются абсолютные пути → path traversal.
- T4.2 ☐ Ограничить размер и тип загрузки (только `application/pdf`).
- T4.3 ☐ `/serve` отдаёт файлы только из песочницы сессии.

**Файлы:** `pdf_editor_web.py`.
**DoD:** запрос с путём `../../etc/passwd` (или `C:\Windows\...`) отклоняется; вне песочницы прочитать нечего.

### T5 — Конфиг через env, bind на localhost 🟠 ☐ TODO
- T5.1 ☐ Запуск на `127.0.0.1:8765` (за nginx); НЕ `0.0.0.0`. Дефолт уже безопасен (`pdf_editor.py:141`).
- T5.2 ☐ Хост/порт/секреты — из env (`.env`, `python-dotenv` уже в стеке), не из открытых аргументов.
- T5.3 ☐ Запуск с `--no-browser` (флаг уже есть).

**DoD:** приложение поднимается одной командой на `127.0.0.1:8765`, конфиг — через env.

### T6 — Деплой под `pdf.runpro.in` 🟠 ☐ TODO
*(зависит от T2, T3, T4, T5; соблюдать Guardrails)*
- T6.1 ☐ Создать DNS-запись **A `pdf.runpro.in` → 13.59.170.121** у DNS-провайдера (бесплатно).
- T6.2 ☐ Развернуть код в отдельном каталоге (напр. `/opt/pdf-editor`), `venv` + `requirements.txt`.
- T6.3 ☐ Запустить app как `pdf-editor.service` на `127.0.0.1:8765` (см. T7).
- T6.4 ☐ Добавить **новый** nginx `server`-блок `pdf.runpro.in` → `proxy_pass http://127.0.0.1:8765;`
  (+ `client_max_body_size` под размер PDF). `sudo nginx -t` → reload. Блок `runpro.in` не трогать.
- T6.5 ☐ Выпустить сертификат: `sudo certbot --nginx -d pdf.runpro.in` (HTTPS, переиспускаем Certbot).
- T6.6 ☐ Проверка с другого ПК: `https://pdf.runpro.in` → авторизация → upload → правка → скачивание.
  Параллельно убедиться, что `https://runpro.in` и его `/api/` продолжают работать.

**DoD:** коллега из браузера открывает `https://pdf.runpro.in`, проходит авторизацию, загружает и
редактирует PDF; существующий сайт/API не затронуты.

### T7 — Автозапуск как сервис 🟡 ☐ TODO
*(зависит от T6.2)*
`systemd`-юнит `pdf-editor.service`: автостарт, `Restart=on-failure`, логи в journald, запуск из `venv`.
**DoD:** после `reboot` сервис поднимается сам; `systemctl status pdf-editor` → `active (running)`.

---

# Журнал выполнения

| Дата | ID | Действие |
|------|-----|----------|
| 2026-06-23 | — | Создан `TASKS.md` как общий трекер. Заведён эпик E1 (публикация в вебе), задачи T1–T8 в TODO. |
| 2026-06-23 | T0 | ✅ Аудит сервера `13.59.170.121` по SSH. Найдено: nginx 80/443 (`runpro.in` + Certbot SSL), Spring Boot `bot.jar`:8080, PostgreSQL:5432, MinIO:9000/9001. Порт 8765 свободен, ресурсов достаточно. Зафиксированы Guardrails. |
| 2026-06-23 | T1 | ✅ Выбран **Вариант A** — публикация на поддомене `pdf.runpro.in` + upload файлов через браузер. Подпуть `/pdf/` отклонён (конфликт `/api/` с Java-бэкендом). Эпик E1 переписан под это; HTTPS свёрнут в T6.5 (certbot). T8 удалён. |
| 2026-06-23 | T2 | ✅ Реализована модель **upload → doc_id**: `/api/upload` (multipart, песочница `_uploads/<doc_id>/`, лимит 50 МБ, проверка `%PDF-`), TTL-очистка 24ч. Все эндпоинты переведены с `path` на токен `doc`; фронтенд — загрузка через `FormData`. Дымовой тест пройден (вкл. negative-кейсы). Обновлены AGENTS.md и .gitignore. |
